# Auth Service

## System Requirements
### Install Dependencies
Run ``npm install`` to download dependencies <br>
Run ``npm install nodemailer`` to download nodemailer dependency <br>

## Running the Service
Run ``npm start``